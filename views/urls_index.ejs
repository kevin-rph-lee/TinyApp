<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <table>
    <tr>
      <th>Short Name</th>
      <th>Long Name</th>
      <th>Delete URL</th>
      <th>Update URL</th>
    </tr>
  <%- include('partials/_header') %>
  <% for(var url in urls) { %>
    <tr>
      <th><%= url %></th>
      <th><a href="http://localhost:8080/u/<%= url %>"><%= urls[url] %></a></th>
      <th>
       <form method="POST" action="/urls/<%= url %>/delete"><input type="submit" name = deleteURL value="Delete"></form>
      </th>
      <th>
        <form action="/urls/<%= url %>/" method="GET"><input type="submit" value="Update"></form>
      </th>
    </tr>
  <% } %>
  </table>
  <form action="/urls/new" method="GET">
    <input type="submit" value="Add URL">
  </form>
</body>
</html>